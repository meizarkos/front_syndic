import 'package:flutter/material.dart';

class WorkRequestCategory extends StatelessWidget {
  const WorkRequestCategory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('WorkRequestCategory'),
      ),
    );
  }
}
