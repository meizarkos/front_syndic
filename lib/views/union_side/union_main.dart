import 'package:flutter/material.dart';

class UnionMain extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Union Main'),
      ),
    );
  }
}
