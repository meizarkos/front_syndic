import 'package:flutter/material.dart';

class ArtisanMain extends StatelessWidget {
  const ArtisanMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Artisan Main'),
      ),
    );
  }
}
